{
  "name": "docker-wordpress",
  "private": true,
  "scripts": {
    "docker:init": "docker compose up -d --build",
    "docker:start": "docker compose start",
    "docker:stop": "docker stop $(docker ps -q) ",
    "db-export": "bash shell/db-export.sh"
  }
}
